# Document loading & chunking
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-experimental>=0.0.60
pypdf>=4.0.0
unstructured>=0.14.0

# Embeddings (HuggingFace — BGE-large)
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0
torch>=2.0.0

# Vector store
langchain-chroma>=0.1.0
chromadb>=0.5.0

# BM25
rank-bm25>=0.2.2

# Ensemble retriever
langchain>=0.2.0
langchain-community==0.3.31

# Reranking (Cohere)
langchain-cohere>=0.1.0
cohere>=5.0.0

# LLM
ollama>=0.6.2

# Evaluation — uses the modern ragas>=0.4 "collections" metrics API
# (the old Dataset.from_dict / functional-metric API was removed)
ragas>=0.4.0
openai>=1.0.0

# Config & CLI
python-dotenv>=1.0.0
click>=8.1.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
tqdm>=4.66.0
